# Agent CLI DevKit システム仕様

## 1. 概要

Agent CLI DevKit は、AI エージェント CLI（Claude Code / Codex CLI / Antigravity CLI / Grok CLI / OpenCode）の開発環境を管理する Electron 製デスクトップアプリケーションである。
UI・内部構造ともに agent を単位としてグループ化し、表示順は Claude / Codex / Agy / Grok / OpenCode とする。画面上の Agy グループは Agy CLI（Antigravity CLI）のみを意味し、別製品の Agy および Antigravity IDE とは区別する。

- プロジェクト名: `agent_cli_devkit`
- 設定ファイルディレクトリ: `~/.agent_cli_devkit`（`src/shared/constants.ts` の `APP_DIR_NAME`。テーマ・言語を `settings.json` として保存する）
- 実行ファイル名: `agent_cli_devkit`
- 配布ファイル名: `agent-cli-devkit-<version>-<os>-<arch>`
- renderer へ公開するグローバル API: `window.agentCliDevkit`

## 2. アーキテクチャ

### 2.1 設計原則

1. **agent 別ファイル分離** — 各 agent の実装が同一ファイルに混在しないよう、層ごとに `claude/` / `codex/` / `agy/` / `grok/` / `opencode/` ディレクトリへ分割する。複数 agent を同時に参照してよいのは「登録ポイント」（registry・合成ファイル）のみ。
2. **共通化** — agent に依存しない処理（WSL 検出、ホームディレクトリのファイル操作、git 実行、サイズ計測、frontmatter/YAML/TOML 編集）は共通モジュールに置き、各 agent から利用する。
3. **拡張容易性** — 新しい agent CLI は「agent 別ディレクトリを追加し、各層の登録ポイントに 1 行追加する」だけで組み込める（§6 参照）。

### 2.2 ディレクトリ構成と責務

```text
src/
├── shared/
│   ├── constants.ts              # アプリ共通定数・共通 IPC チャンネル (window/system/updater)
│   ├── types.ts                  # アプリ共通型 (OSType / UpdateState)
│   ├── ipc.ts                    # window.agentCliDevkit の型 (agent API を束ねる登録ポイント)
│   └── agents/
│       ├── types.ts              # agent 非依存のドメイン型 (AgentEnvironment / MCP / Cleanup / Asset / Settings)
│       ├── claude/constants.ts   # Claude の定数・IPC チャンネル・レジストリ (クリーンアップ候補/設定項目)
│       ├── claude/types.ts       # Claude 固有型 + 共通型の再エクスポート
│       ├── claude/ipc.ts         # ClaudeIpcApi (window.agentCliDevkit.claude の型)
│       ├── codex/…               # Codex も同構成
│       ├── agy/…                 # Antigravity CLI も同構成
│       ├── grok/…                # Grok も同構成
│       └── opencode/…            # OpenCode も同構成
├── main/
│   ├── index.ts                  # 起動・ウィンドウ生成 (sandbox 無効・contextIsolation 有効)
│   ├── ipc/
│   │   ├── index.ts              # 登録ポイント: WslDetector / AgentCliRunner を生成し各 agent へ注入
│   │   ├── claude/               # Claude 系 IPC ハンドラ (desktop/code/cleanup/asset/settings/plugin)
│   │   ├── codex/                # Codex 系 IPC ハンドラ (mcp/asset/cleanup/config/plugin)
│   │   ├── agy/                  # Antigravity CLI 系 IPC ハンドラ
│   │   ├── grok/                 # Grok 系 IPC ハンドラ (mcp/asset/cleanup/config/plugin)
│   │   ├── opencode/             # OpenCode 系 IPC ハンドラ
│   │   ├── systemHandlers.ts     # system:* (テーマ/ロケール/バージョン)
│   │   ├── appSettingsHandlers.ts # app-settings:* (~/.agent_cli_devkit/settings.json の読み書き)
│   │   ├── windowHandlers.ts     # window:* (最小化/最大化/閉じる)
│   │   └── updater.ts            # updater:* (自動アップデート)
│   ├── services/
│   │   ├── claude/               # ClaudeDesktopManager / ClaudeCodeManager / ClaudeCleanupManager
│   │   │                         # / ClaudeAssetManager / ClaudeSettingsManager / ClaudePluginManager
│   │   ├── codex/                # CodexMcpManager / CodexAssetManager / CodexCleanupManager / CodexConfigManager
│   │   │                         # / CodexPluginManager
│   │   ├── agy/                  # Agy の MCP/Asset/Cleanup/Settings/Plugin マネージャ
│   │   ├── grok/                 # GrokMcpManager / GrokAssetManager / GrokCleanupManager / GrokConfigManager
│   │   │                         # / GrokPluginManager
│   │   ├── opencode/             # OpenCode の MCP/Asset/Cleanup/Settings/Plugin マネージャ
│   │   ├── common/
│   │   │   ├── assets/MarkdownAssetManager.ts # md agent / SKILL.md の共通管理
│   │   │   ├── cleanup/AgentCleanupManager.ts # 宣言的クリーンアップ
│   │   │   ├── json/             # JSON/JSONC MCP・設定の差分編集
│   │   │   ├── wsl/WslDetector.ts  # WSL distro 検出 (agent 非依存、判定コマンドは呼び出し側が渡す)
│   │   │   ├── wsl/HomeFs.ts       # native/WSL のホーム配下ファイル操作の抽象化 (UNC/コマンドモード)
│   │   │   ├── cli/AgentCliRunner.ts # agent CLI のヘッドレス実行 (native/WSL 両対応、引数検査つき)
│   │   │   └── git/GitRunner.ts    # git clone/pull 実行
│   │   └── updater.ts            # electron-updater ラッパー
│   └── utils/                    # frontmatter / fsSize / yamlPreserve / tomlEdit / jsoncEdit / console-bridge
├── preload/
│   ├── index.ts                  # 登録ポイント: 共通 API + agent API を window.agentCliDevkit に公開
│   └── agents/*.ts               # agent 別 IPC ブリッジ
└── renderer/
    ├── App.tsx                   # ルート定義 (registry から生成)
    ├── agents/
    │   ├── types.ts              # AgentModule / AgentFeature 型
    │   ├── index.ts              # 登録ポイント: Claude / Codex / Agy / Grok / OpenCode の順
    │   ├── claude/               # Claude 系画面 (index.tsx = モジュール定義 + 各コンポーネント)
    │   ├── codex/                # Codex 系画面
    │   ├── agy/                  # Antigravity CLI 系画面
    │   ├── grok/                 # Grok 系画面
    │   └── opencode/             # OpenCode 系画面
    ├── components/               # 共通 UI (TitleBar / Dashboard / AppSettings / Layout / UpdateNotification)
    │   ├── agent/                # MCP / Agent・Skill / 設定 / クリーンアップの共通画面 (agent 非依存。Agy / OpenCode が使用)
    │   ├── assets/               # Agent・Skill 一覧の共通部品 (AssetSearchField / AssetEntriesTable)
    │   ├── settings/             # 設定値エディタ (SettingsValueEditor)
    │   └── plugins/              # プラグイン管理の共通画面 (PluginManagerView ほか。agent 非依存)
    ├── i18n/locales/{ja,en}/     # app.ts (共通文言) + agent 別文言 + index.ts (合成)
    ├── store/useAppStore.ts      # テーマ・言語の zustand ストア
    └── utils/format.ts           # 表示整形 (サイズ/日時/環境キー)
```

### 2.3 プロセス構成と IPC

- メインプロセス起動時に `registerIpcHandlers()`（`src/main/ipc/index.ts`）が `WslDetector` と `AgentCliRunner` を 1 つずつ生成し、各 agent のマネージャに注入して IPC ハンドラを登録する。
- preload は `sandbox: false`（`contextIsolation: true` は維持）で動作し、agent 別ブリッジファイル（`preload/agents/*.ts`）を require 合成して `window.agentCliDevkit` を公開する。sandbox を無効化しているのは preload を agent 別ファイルに分割するためで、shared の定数・型を preload から直接 import できる利点もある。
- IPC チャンネルは agent プレフィックスで名前空間化されている:
    - Claude: `claude-desktop:*` / `claude-code:*` / `claude-cleanup:*` / `claude-asset:*` / `claude-settings:*` / `claude-plugin:*`
    - Codex: `codex-mcp:*` / `codex-asset:*` / `codex-cleanup:*` / `codex-config:*` / `codex-plugin:*`
    - Agy: `agy-mcp:*` / `agy-asset:*` / `agy-cleanup:*` / `agy-settings:*` / `agy-plugin:*`
    - Grok: `grok-mcp:*` / `grok-asset:*` / `grok-cleanup:*` / `grok-config:*` / `grok-plugin:*`
    - OpenCode: `opencode-mcp:*` / `opencode-asset:*` / `opencode-cleanup:*` / `opencode-settings:*` / `opencode-plugin:*`
    - 共通: `window:*` / `system:*` / `app-settings:*` / `updater:*` / `main:console`

### 2.4 renderer API（window.agentCliDevkit）

```text
window.agentCliDevkit
├── system   { getTheme, getLocale, getVersion, openExternal }
├── appSettings { read, write }   # ~/.agent_cli_devkit/settings.json
├── window   { minimize, maximize, close, isMaximized }
├── updater  { getState, check, download, quitAndInstall, onStateChanged }
├── claude   { desktop, code, cleanup, asset, settings, plugin }   # ClaudeIpcApi
├── codex    { mcp, asset, cleanup, config, plugin }               # CodexIpcApi
├── agy      { mcp, asset, cleanup, settings, plugin }             # AgyIpcApi
├── grok     { mcp, asset, cleanup, config, plugin }               # GrokIpcApi
└── opencode { mcp, asset, cleanup, settings, plugin }             # OpenCodeIpcApi
```

## 3. 機能仕様

### 3.1 Claude グループ

| 機能                          | ルート                | 内容                                                                                                                                                                                                                                                                |
| ----------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Claude Desktop MCP 管理       | `/claude/desktop-mcp` | `claude_desktop_config.json` の mcpServers を有効/無効/並べ替え。無効分は同ディレクトリの `claude_desktop_config_disabled.json` へ退避。Claude Desktop の再起動に対応。                                                                                         |
| Claude Code MCP 管理          | `/claude/code-mcp`    | `~/.claude.json` のトップレベル mcpServers を管理。無効分は `~/.claude-disabled-mcp.json`（sidecar）へ退避。native + Claude 導入済み WSL distro が対象。                                                                                                            |
| Claude Code Agent・Skill 管理 | `/claude/agent-skill` | `~/.claude/agents`（.md）・`~/.claude/skills`（フォルダ）の一覧/ZIP ダウンロード/ZIP・md アップロード/削除。Anthropic 公式スキル（anthropics/skills）のインポート。                                                                                                 |
| Claude Code プラグイン管理    | `/claude/plugins`     | プラグインの一覧/インストール/アンインストールとマーケットプレイスの追加/削除。`claude plugin` CLI のヘッドレス実行（一覧・カタログは `--json` 出力）。個別リポジトリは「マーケットプレイスとして追加 → 含まれるプラグインを選択」の 2 段階。                       |
| Claude Code 設定              | `/claude/settings`    | `~/.claude/settings.json` の登録項目をテーブル編集・直接編集（関係ない項目には触れない差分マージ）。                                                                                                                                                                |
| Claude Code クリーンアップ    | `/claude/cleanup`     | `~/.claude` 配下の履歴/キャッシュ/一時/ログ（projects・plans・file-history・history.jsonl・shell-snapshots・cache・debug・sessions・session-env・tasks・backups）を選択削除。「その他のツール」として Serena（~/.serena）の登録プロジェクト初期化・ログ削除に対応。 |

### 3.2 Codex グループ

| 機能                    | ルート               | 内容                                                                                                                                                                                                                                                                                         |
| ----------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Codex MCP 管理          | `/codex/mcp`         | `~/.codex/config.toml` の `[mcp_servers.*]` を有効/無効/並べ替え（TOML の書式・コメントを保持）。無効分は `~/.codex/config-disabled-mcp.toml` へ退避。native + Codex 導入済み WSL distro が対象。                                                                                            |
| Codex Agent・Skill 管理 | `/codex/agent-skill` | `~/.codex/agents`（.toml）・`~/.agents/skills`（フォルダ）の一覧/ZIP ダウンロード/ZIP・ファイルアップロード/削除。OpenAI 公式スキル（openai/skills、カタログは skills/.curated 配下）のインポート。                                                                                          |
| Codex プラグイン管理    | `/codex/plugins`     | プラグインの一覧/インストール/アンインストールとマーケットプレイスの追加/削除。`codex plugin` CLI のヘッドレス実行（全操作 `--json` 対応）。OpenAI 組み込みマーケットプレイス（openai-bundled / openai-primary-runtime / openai-curated）は削除不可として表示。個別リポジトリは 2 段階方式。 |
| Codex 設定              | `/codex/settings`    | `~/.codex/config.toml` の登録項目をテーブル編集・直接編集（関係ない項目には触れない差分マージ）。                                                                                                                                                                                            |
| Codex クリーンアップ    | `/codex/cleanup`     | `~/.codex` 配下（sessions・cache・generated_images・tmp・log・models_cache.json・session_index.jsonl）を選択削除。「その他のツール」（Serena）にも対応。                                                                                                                                     |

### 3.3 Agy グループ（Antigravity CLI）

対象製品は Agy CLI（Antigravity CLI）のみである。別製品の Agy / Antigravity IDE の設定・Skill・Plugin を Agy CLI 固有データとして混在させない。一方、Agy CLI 公式仕様で Gemini CLI と共有すると定義されたグローバル領域は Agy CLI の正式な管理対象とする。

| 機能                      | ルート             | 内容                                                                                                                                                                                                                                                                    |
| ------------------------- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Agy CLI MCP 管理          | `/agy/mcp`         | Gemini 共通グローバル設定 `~/.gemini/config/mcp_config.json` の `mcpServers` を管理する。無効分は同ディレクトリの `mcp_config_disabled.json` へエントリごと退避し、オブジェクト順で並べ替える。workspace の `.agents/mcp_config.json` は対象外。                                                          |
| Agy CLI Agent・Skill 管理 | `/agy/agent-skill` | Custom Agent は Gemini 共通 `~/.gemini/config/agents/<name>/agent.md`、Skill は Agy CLI で唯一対象にする `~/.gemini/antigravity-cli/skills/<name>/SKILL.md` を一覧/参照/ファイルマネージャー表示/ZIP DL/ZIP・md UL/削除する。`.agents` / `.claude` の互換領域は対象外。 |
| Agy CLI Plugin 管理       | `/agy/plugins`     | `~/.gemini/config/plugins` のディレクトリ走査で一覧を構築し（`agy plugin list` が列挙しない共通/bundled Plugin も含む）、`agy plugin install/uninstall/enable/disable` で変更する。Gemini CLI と共通であることを理由に除外しない。Agy CLI 1.1.3 は機械可読カタログ・マーケットプレイス管理を公開しないため、画面は直接ソース導入のみ表示する。              |
| Agy CLI 設定              | `/agy/settings`    | `~/.gemini/antigravity-cli/settings.json` を編集する。スカラーはテーブル編集し、配列・マップ・オブジェクトは直接編集行のみ表示する。キーバインドは Claude 対応機能ではないため対象外。                                                                                  |
| Agy CLI クリーンアップ    | `/agy/cleanup`     | `~/.gemini/antigravity-cli` の cache/log/crashes/cli.log/conversations/brain/会話要約 DB/history.jsonl/update.lock を選択削除する。settings.json、skills、bin、builtin、mcp、Gemini 共通設定は保護する。                                                                |

### 3.4 Grok グループ

対象は `~/.grok` 配下のみ（CLI 横断の共有ディレクトリ `~/.agents/skills` や、Grok が互換読み込みする `~/.claude` 系設定には触れない）。

| 機能                   | ルート              | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Grok MCP 管理          | `/grok/mcp`         | `~/.grok/config.toml` の `[mcp_servers.*]` を有効/無効/並べ替え（TOML の書式・コメントを保持）。無効分は `~/.grok/config-disabled-mcp.toml` へ退避。native + Grok 導入済み WSL distro が対象。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Grok Agent・Skill 管理 | `/grok/agent-skill` | `~/.grok/agents`（直下の .md のみ・非再帰。Grok CLI の実挙動に準拠。サブディレクトリを含む ZIP のアップロードは孤児ファイル防止のため拒否）・`~/.grok/skills`（フォルダ）の一覧/ZIP ダウンロード/ZIP・md アップロード/削除。xAI 公式スキル（xai-org/plugin-marketplace、カタログは default-skills 配下）のインポート。                                                                                                                                                                                                                                                                                                                                                                                  |
| Grok プラグイン管理    | `/grok/plugins`     | プラグインの一覧/インストール/アンインストールとマーケットプレイスソースの追加/削除。`grok plugin` CLI のヘッドレス実行（install は `--trust`、uninstall は `--confirm` を付与）。個別リポジトリ/ローカルパスからの直接インストールに対応（@ref / #subdir 可）。マーケットプレイスからのインストールは `<plugin>@<qualifier>`（local ソース= `local/<name>`、git ソース= `owner/repo`）を組み立てて実行。Claude 由来の合成マーケットプレイス（config.toml の `[[marketplace.sources]]` に無いもの）は「他ツール由来」として削除不可表示。grok 0.2.93 の不具合対策として、ローカルソースの remove 失敗時は config.toml から該当ブロックを直接除去、uninstall 後は `[plugins] enabled` の残骸を掃除する。 |
| Grok 設定              | `/grok/settings`    | `~/.grok/config.toml` の登録項目をテーブル編集・直接編集（関係ない項目には触れない差分マージ）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Grok クリーンアップ    | `/grok/cleanup`     | `~/.grok` 配下（sessions・memory・debug・plans・docs・sandbox-events.jsonl）と `bin` 配下の旧バージョンバイナリ（`grok-<version>` 形式のうち最新以外。起動用 `grok` / `grok.exe` と最新バージョンは保護）を選択削除。「その他のツール」（Serena）にも対応。                                                                                                                                                                                                                                                                                                                                                                                                                                             |

### 3.5 OpenCode グループ

対象は OpenCode 自身の公式グローバル/XDG領域だけである。OpenCode が互換読み込みする `~/.claude` / `~/.agents` とworkspaceの `.opencode` は管理しない。

| 機能                       | ルート                  | 内容                                                                                                                                                                                                                         |
| -------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenCode MCP 管理          | `/opencode/mcp`         | `~/.config/opencode/opencode.json` のトップレベル `mcp` を管理する。無効分は `~/.config/opencode/opencode-disabled-mcp.json`（sidecar）へエントリごと退避し、ローカルの配列 `command` とリモート `url` の両形式を表示する。JSONC の無関係なキーとコメントを保持する。          |
| OpenCode Agent・Skill 管理 | `/opencode/agent-skill` | `~/.config/opencode/agents/*.md` と `~/.config/opencode/skills/<name>/SKILL.md` を一覧/参照/ファイルマネージャー表示/ZIP DL/ZIP・md UL/削除する。互換用 `~/.claude/skills` / `~/.agents/skills` は対象外。                   |
| OpenCode Plugin 管理       | `/opencode/plugins`     | `opencode.json` の `plugin` 配列にある npm package と `~/.config/opencode/plugins` の `.js`・`.ts` を一覧/導入/削除/有効化/無効化する。npm の無効項目は `disabled-npm-plugins.json`、ローカル無効項目は `plugins-disabled/` に退避し、再有効化可能にする。公式のカタログ/マーケットプレイス管理はないため表示しない。 |
| OpenCode 設定              | `/opencode/settings`    | `opencode.json` のトップレベル単一スカラーをテーブル編集し、配列・マップ・オブジェクト・union 型は直接編集行のみ表示する。JSONCコメントと末尾カンマを検証し、対象値以外を保持する。TUI専用 `tui.json` と keybinds は対象外。 |
| OpenCode クリーンアップ    | `/opencode/cleanup`     | 公式XDG領域の `~/.cache/opencode`、`~/.local/share/opencode/{log,repos,opencode.db*}`、`~/.local/state/opencode/locks` を選択削除する。config、Agent、Skill、Plugin、認証情報、モデル選択は保護する。                        |

### 3.6 共通機能

- **ダッシュボード** — agent ごとに Claude / Codex / Agy / Grok / OpenCode の固定順でアコーディオンを表示し、配下に全機能のカードを並べる。アコーディオンは排他展開（1 つ開くと他が閉じる）で、最後の展開状態を localStorage へ保存し次回起動時に復元する（初回は先頭グループを展開）。Agy グループの見出しのみ `dashboardLabel`（「Agy（Antigravity CLI）」）で補足表示する。カードはアイコンを左・テキストを右に置く横並びのコンパクトレイアウト。`renderer/agents/index.ts` の registry から自動生成。
- **タイトルバー** — ダッシュボードアイコンに続き、agent グループの代表ボタンを Claude / Codex / Agy / Grok / OpenCode の順で表示するアコーディオン方式。代表ボタンをクリックするとそのグループの機能アイコン（通常より一回り小さい 22px アイコン）が展開され、別グループを展開すると先に開いていたグループは自動で閉じる（同じボタンの再クリックで閉じる）。画面遷移時は遷移先の機能が属するグループを自動展開する。右側にはアプリ設定アイコン（ラベルなし）とバーガーメニューを置く。バーガーメニューは「ダッシュボード / agent グループ（ホバーで機能一覧をカスケード表示する 2 階層メニュー。右側に展開し、収まらない場合は自動で左へ反転）/ アプリ設定 / 終了」で構成する。
- **アプリ設定** — `/app-settings` でテーマ（ライト/ダーク）と言語（日本語/英語）を編集する。編集値はドラフトとして保持し、「保存」を押した時点でアプリへ反映して `~/.agent_cli_devkit/settings.json` へ永続化する。保存値がない初回起動時は OS のテーマ・言語設定を引き継ぐ。
- **自動アップデート** — GitHub Releases を対象に electron-updater でチェック/ダウンロード/適用。
- **WSL 対応** — Windows では WSL distro を検出し、対象 CLI が導入された distro を各機能の管理対象として表示。ファイルアクセスは `\\wsl.localhost\` / `\\wsl$\` の UNC を優先し、不可なら wsl コマンド実行にフォールバック。
- **Agent・Skill の ZIP ダウンロード** — 既定ファイル名は `<agent>_<kind>[-<distro>].zip`（例: claude_skills.zip / grok_agents-Ubuntu-24.04.zip）。
- **MCP 管理（共通レイアウト）** — 全 agent の MCP 画面は同一レイアウト。ヘッダーに設定ファイルと無効化ファイルの両パスを表示し、有効/無効の 2 テーブル（列: ドラッグハンドル / サーバー名 / 操作トグル / コマンド / 引数）を `tableLayout: fixed` で画面幅比率から配分する（引数列が最大）。長い値は省略（…）せず自動改行し、行のドラッグ&ドロップで並べ替える。無効化はフラグ書き換えではなく、エントリ/ブロックごと各 agent の無効化ファイルへ退避する方式で統一する。
- **Agent・Skill 一覧の検索** — 各 agent のエージェントタブとスキルタブでは、名前または説明による絞り込みに対応する。検索語は空白区切り（順不同）の AND 条件とし、全検索語が名前内、または全検索語が説明内に含まれる項目を表示する。大文字小文字および全角半角の違いは区別しない。検索条件と一括操作の対象はタブごとに独立し、一括操作は現在の検索結果に表示されている選択項目だけに適用する。
- **Agent・Skill 一覧のファイルマネージャー表示** — 名前の右にある外部リンクアイコンから、エージェントは対象ファイルを OS 標準のファイルマネージャー内で選択表示し、スキルは対象ディレクトリを開く。native はホスト OS の絶対パス、WSL は `HomeFs` が解決した UNC パスを `<agent>-asset:reveal-entry` で Electron の `shell` へ渡す。main 側で種別と相対パスを検証し、管理対象外のパスは開かない。
- **Agent・Skill 一覧の参照列** — 各行に「ヘッダ」（frontmatter のみ）と「全体」（ファイル内容全体。agents=ファイル本体 / skills=SKILL.md。IPC `<agent>-asset:read-entry`）の 2 つの参照ボタンを 2 行で表示する。
- **設定の直接編集ダイアログ** — ダイアログを固定高（85vh）とし、スクロールはテキストエリア内の 1 本のみ（ネストスクロールなし）。
- **プラグイン管理（共通実装）** — 画面本体は agent 非依存の `renderer/components/plugins/PluginManagerView` で、各 agent の画面は API と文言キーを渡す薄いラッパー。機能差（直接インストール、npm package、カタログ、マーケットプレイス管理、リモート marketplace URL）は `PLUGIN_CAPABILITIES` に宣言する。カタログ/マーケットプレイス非対応のAgy/OpenCodeでは該当タブや操作を表示せず、直接ソース導入だけを表示する。CLI変更操作は `AgentCliRunner` を使う。OpenCodeは公式CLIにPlugin管理コマンドがないため、公式設定ファイル/ディレクトリを差分編集する。詳細は §3.7 参照。
- **共通画面の再利用** — Agy / OpenCode の MCP / Agent・Skill / 設定 / クリーンアップ画面は agent 非依存の共通ビュー（`renderer/components/agent/`）で実装し、既存 agent（Claude / Codex / Grok）の画面と同一のレイアウト・操作性・文言スタイルを保つ。設定画面の directEdit 項目は値欄に「ファイルを直接編集」のテキストのみを表示する（リンク・ボタンにしない）。
- **i18n** — 日本語/英語。共通文言は `locales/<lang>/app.ts`、agent 固有文言は `locales/<lang>/<agent>.ts` に分離し、`<agent>.*` の名前空間で参照する。日本語は英単語・パスの前後に半角スペースを置き、本文・タブ・設定ラベルはカタカナ表記（エージェント/スキル/プラグイン）で統一する。

### 3.7 プラグイン管理の CLI 連携仕様

各 agent CLI のプラグイン仕様（Claude/Codex/Grokは2026-07-13、Agy CLI 1.1.3 / OpenCode 1.18.3は2026-07-17に実機・公式資料で検証）。

**実行するコマンド（マネージャごとの対応表）:**

| 操作                   | Claude                                    | Codex                                                                                 | Grok                                                     |
| ---------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| インストール済み一覧   | `claude plugin list --json`               | `codex plugin list --json`                                                            | `grok plugin list --json`                                |
| カタログ               | `claude plugin list --available --json`   | `codex plugin list --available --json`                                                | `grok plugin list --available --json`                    |
| マーケットプレイス一覧 | `claude plugin marketplace list --json`   | `codex plugin marketplace list --json`                                                | `grok plugin marketplace list --json`                    |
| インストール           | `claude plugin install <p>@<market>`      | `codex plugin add <p>@<market> --json`                                                | `grok plugin install <p>@<qualifier> --trust`            |
| 個別リポジトリから     | 非対応（2 段階方式）                      | 非対応（2 段階方式）                                                                  | `grok plugin install <src> --trust`（@ref / #subdir 可） |
| アンインストール       | `claude plugin uninstall <id>`            | `codex plugin remove <id> --json`                                                     | `grok plugin uninstall <name> --confirm`                 |
| 有効/無効切り替え      | `claude plugin enable\|disable <id>`      | サブコマンド無し → config.toml の `[plugins."<id>"] enabled` を直接編集（対象行のみ） | `grok plugin enable\|disable <name>`                     |
| マーケットプレイス追加 | `claude plugin marketplace add <src>`     | `codex plugin marketplace add <src> --json`                                           | `grok plugin marketplace add <src>`                      |
| マーケットプレイス削除 | `claude plugin marketplace remove <name>` | `codex plugin marketplace remove <name>`                                              | `grok plugin marketplace remove <name>` + フォールバック |

**ソース種別と互換性:**

- Agy CLI: `agy plugin install <target>` / `uninstall <name>` / `enable|disable <name>` を使う。`~/.gemini/config/plugins` はGemini CLI共通だがAntigravity CLIの正式対象である。一覧はディレクトリ走査のみで構築する（`agy plugin list` が列挙しない共通/bundled Plugin も含まれる）。カタログとマーケットプレイスUIは提供しない。
- OpenCode: npm package は `opencode.json` の `plugin` 配列、ローカルPluginは `~/.config/opencode/plugins/*.js|*.ts` が公式入力である。OpenCodeが起動時にBunでnpm依存関係を導入するため、本アプリはpackage名の設定だけを行う。リポジトリ形式のカタログ/マーケットプレイスUIは提供しない。

- マーケットプレイスソース: Claude = owner/repo / Git URL（`#ref` 可）/ ローカルパス / リモート marketplace.json URL。Codex = ローカルパス / `owner/repo[@ref]` / HTTPS・SSH Git URL（URL 直接指定は不可）。Grok = Git URL / owner/repo / ローカルディレクトリ。
- **3 CLI とも Claude 形式のマニフェスト（`.claude-plugin/marketplace.json` + `plugin.json`）を読める**（実機検証済み）。このため「個別リポジトリからの追加」の 2 段階方式（マーケットプレイスとして追加 → 含まれるプラグインを選択）は同一リポジトリで 3 CLI 共通に運用できる。
- 組み込みマーケットプレイス（削除不可扱い）: Claude = `claude-plugins-official`、Codex = `openai-bundled` / `openai-primary-runtime` / `openai-curated`、Grok = `xAI Official`。定義は各 `constants.ts` の `*_BUILTIN_MARKETPLACES`。

**データ配置（表示は CLI 出力を正とし、以下は参考・フォールバック用）:**

- Claude: 実体 `~/.claude/plugins/cache/<market>/<plugin>/<version>/`、登録 `~/.claude/plugins/known_marketplaces.json` / `installed_plugins.json`、有効状態 `~/.claude/settings.json` の `enabledPlugins`（`{"<p>@<m>": bool}`）と `extraKnownMarketplaces`。
- Codex: `~/.codex/config.toml` の `[marketplaces.<name>]`（`source_type` / `source` / `last_updated`）と `[plugins."<p>@<m>"] enabled`。実体 `~/.codex/plugins/cache/`。`marketplace list` には config.toml に無い組み込みも出るため config 解析では不足する。
- Codex のカタログ補完: `plugin list --available --json` には description が含まれないため、各エントリの `source.path`（マーケットプレイススナップショット内のプラグイン実体）にある `.codex-plugin/plugin.json` から description / homepage を best-effort で補完する（native は直接読み、WSL は `\\wsl.localhost` / `\\wsl$` の UNC で読む。全エントリ並列で数百 ms 程度）。
- Grok: `~/.grok/config.toml` の `[[marketplace.sources]]`（ローカル= `path`、Git= `git` キー）と `[plugins] enabled = [...]`。実体 `~/.grok/installed-plugins/`。Claude の `known_marketplaces.json` を自動読込して一覧へ合成する（config.toml に無いソース = 他ツール由来として削除不可表示）。

**カタログのリンクと選択可否:**

- カタログ各エントリはリポジトリ URL（`homepage`）を持ち、追加ダイアログのプラグイン名直後の
  外部リンクアイコン（ラベルなし）のクリックで OS 既定ブラウザで開く
  （`system:open-external`。http/https のみ許可）。
  導出方法: Claude = カタログの `source`（github は repo、git-subdir は URL + `/tree/<ref>/<path>`）、
  Codex = plugin.json の `homepage` / `interface.websiteURL` / `repository`、
  Grok = プラグイン単位の URL が無いためマーケットプレイスの Git URL で代用。
- `PluginMarketplaceEntry.selectable` はプラグイン追加ダイアログでインストール元として
  選択可能かを示す。Grok では Claude 由来の外部マーケット（`origin='external'`）のうち
  公式 `claude-plugins-official`（`GROK_SELECTABLE_EXTERNAL_MARKETPLACES`）のみ true とし、
  ユーザーが Claude 用に追加した外部マーケットは Grok のインストール候補・カタログから除外する。

**Grok 固有の注意（0.2.93 実機で確認した不具合と対処）:**

1. マーケットプレイス指定子は表示名ではなく qualifier（ローカル= `local/<name>`、Git= `owner/repo`）。`GrokPluginManager.catalog()` が marketplace list の kind / URL から組み立てる。
2. `marketplace remove` はローカルソースを削除できない（exit 126）→ 失敗時に config.toml の該当 `[[marketplace.sources]]` ブロックを直接除去するフォールバックを実装。
3. `uninstall` 後に `[plugins] enabled` 配列へ名前が残ることがある → 成功後に該当エントリを掃除する。

**タイムアウト:** 一覧 60 秒 / 変更操作 180 秒（Claude 内部の git タイムアウト既定 120 秒
`CLAUDE_CODE_PLUGIN_GIT_TIMEOUT_MS` より長く設定）。private リポジトリの取得は各 CLI が参照する
`GITHUB_TOKEN` / `GITLAB_TOKEN` / `BITBUCKET_TOKEN` 等の環境変数に依存する。

## 4. 設定編集レジストリ

- Claude: `shared/agents/claude/constants.ts` の `SETTINGS_FIELDS`（`~/.claude/settings.json` の表示・編集対象項目）。envFlag 型（env 内キーの ON/OFF）に対応。
- Codex: `shared/agents/codex/constants.ts` の `SETTINGS_FIELDS`（`~/.codex/config.toml` のドット区切りキー）。
- Agy: `shared/agents/agy/constants.ts` の `SETTINGS_FIELDS`（`~/.gemini/antigravity-cli/settings.json` のトップレベルキー）。Antigravity IDE や互換ディレクトリの設定を含めない。
- Grok: `shared/agents/grok/constants.ts` の `SETTINGS_FIELDS`（`~/.grok/config.toml` のドット区切りキー。公式 settings リファレンス準拠）。
- OpenCode: `shared/agents/opencode/constants.ts` の `SETTINGS_FIELDS`（`~/.config/opencode/opencode.json` のトップレベルキー）。互換ディレクトリと TUI 専用設定を含めない。
- いずれも registry へ 1 項目追加すると UI 表示へ反映される。型は boolean / string / number / Claude の envFlag に加え、配列・オブジェクト・動的テーブルなど一覧表示専用の directEdit を扱う。編集可能型は読み取り・テーブル編集・保存まで反映され、整数制約と数値範囲を宣言できる。
- 選択候補は二種類ある。厳密な列挙値はセレクト、モデル ID・テーマ・プロバイダ ID など拡張可能な値は候補付き自由入力として表示する。厳密な列挙値でも、将来の CLI や外部編集で保存された候補外値を空欄にせず「保存済み値」として表示する。
- 選択候補の先頭は未設定を表す。公式資料または公式ソースで固定既定値を確認できた項目は「未設定（既定: 有効/無効/<値>）」と表示し、環境依存・モデル依存・未記載の項目は「未設定」のまま表示する。文字列・数値・候補付き自由入力には未設定や入力例の placeholder を表示しない。
- 2026-07-17 時点の表示対象は Claude 91 項目（編集可能 68 / 直接編集 23）、Codex 107 項目（編集可能 86 / 直接編集 21）、Agy 18 項目（編集可能 14 / 直接編集 4）、Grok 83 項目（編集可能 68 / 直接編集 15）、OpenCode 31 項目（編集可能 9 / 直接編集 22）。各項目は日本語・英語の双方で短い名称と項目固有の説明を必須とし、キーだけのタイトルや汎用説明へのフォールバックは使用しない。
- テーブル保存は、読み込み時の値と比較してユーザーが変更した編集可能値だけを main プロセスへ送る。main 側も入力に存在する編集可能項目だけを書き戻し、directEdit は renderer / main の双方で保存対象外にする。この二重の差分適用により、未対応の新規設定、TOML の inline table、JSON/TOML のオブジェクト・配列を別項目の保存で削除しない。
- 文字列・数値入力を空欄にすると renderer は変更値を `undefined` として送る。Claude / Agy / OpenCode は対象 JSON キー（Claude の envFlag は対象 env キー）を削除し、Codex / Grok は対象 TOML scalar を削除するため、空文字列としては保存せず未設定へ戻す。編集前から未設定の空欄は差分に含めない。
- JSON/TOML のオブジェクト、配列、動的テーブル、Codex の granular approval policy など構造値は設定一覧にも項目として表示し、値欄へ「ファイルを直接編集」と表示する。画面上部には編集方式を説明する全体メッセージを表示しない。直接編集はファイル全体を構文検証して保存する。通常保存でも既存 JSON/TOML が壊れている場合は書き込みを拒否する。
- Claude の `model` / `advisorModel` は公式 alias、正式 Anthropic model ID、Bedrock ARN、Foundry deployment、Agent Platform version、ゲートウェイ固有 ID を受け付ける。Codex と Grok のモデルも動的カタログまたはカスタムプロバイダを考慮し、候補外 ID を許可する。
- 設定項目・候補・既定値は 2026-07-17 時点の Claude Code、Codex、Antigravity CLI、Grok Build、OpenCode の公式設定リファレンスおよび実機スキーマに合わせている。Claude の非推奨 `includeCoAuthoredBy` はテーブル対象外とし、構造値の `attribution` は直接編集で扱う。Agy / OpenCode とも配列・マップ・オブジェクトは直接編集だけを表示し、Claude 対応機能ではないキーバインド設定は追加しない。

## 5. データモデル

本アプリはデータベースを使用しない。操作対象は各 CLI ツールの設定ファイル（JSON / TOML / YAML）とディレクトリである。アプリが管理する永続データは次のみとする。

- MCP の無効化ファイル（sidecar）: Claude Desktop `claude_desktop_config_disabled.json` / Claude Code `~/.claude-disabled-mcp.json` / Codex `~/.codex/config-disabled-mcp.toml` / Agy `~/.gemini/config/mcp_config_disabled.json` / Grok `~/.grok/config-disabled-mcp.toml` / OpenCode `~/.config/opencode/opencode-disabled-mcp.json`。
- OpenCode Plugin の無効状態（公式仕様に保存項目がないため）: `~/.config/opencode/disabled-npm-plugins.json` と `~/.config/opencode/plugins-disabled/`。
- アプリ設定（`~/.agent_cli_devkit/settings.json`）: テーマ `theme` / 言語 `language`。
- ダッシュボードの展開グループ: renderer の localStorage（`dashboard.expandedAgent`）。

## 6. agent 追加ガイド

新しい agent CLI（例: `gemini`）を追加する場合、以下の「agent 別ディレクトリの追加 + 登録ポイントへの 1 行追加」で完結する。

1. **shared** — `src/shared/agents/gemini/{constants,types,ipc}.ts` を作成。
    - 登録: `src/shared/ipc.ts` の `IpcApi` に `gemini: GeminiIpcApi;` を追加。
2. **main services** — `src/main/services/gemini/` にマネージャを実装。共通処理は `services/common/`（`WslDetector.getDistrosWithTest()` / `HomeFs`）と `main/utils/` を利用。
3. **main ipc** — `src/main/ipc/gemini/` にハンドラと `index.ts`（`registerGeminiIpcHandlers`）を作成。
    - 登録: `src/main/ipc/index.ts` に 1 行追加。
4. **preload** — `src/preload/agents/gemini.ts` にブリッジを作成。
    - 登録: `src/preload/index.ts` の api に `gemini: geminiApi,` を追加。
5. **renderer** — `src/renderer/agents/gemini/` に画面コンポーネントと `index.tsx`（`AgentModule` 定義: ルート/アイコン/文言キー）を作成。
    - 登録: `src/renderer/agents/index.ts` の `AGENT_MODULES` に 1 行追加。ダッシュボード・タイトルバー・ルートは自動反映される。
6. **i18n** — `src/renderer/i18n/locales/{ja,en}/gemini.ts` を作成し、各 `index.ts` に 1 行追加。文言キーは `gemini.*` 名前空間。

## 7. ビルド・配布

- 開発: `yarn dev`（tsc watch + vite + electron。renderer は BrowserRouter / <http://localhost:3001>）
- 検証: `yarn lint`（tsc --noEmit）/ `yarn eslint`
- 配布: `yarn dist:win|mac|linux`（renderer は HashRouter / `dist/renderer/index.html`）
- リリース: `yarn release:win|mac|linux`（GitHub ドラフトリリースへアップロード）

## 8. セキュリティ上の注意

- BrowserWindow は `contextIsolation: true` / `nodeIntegration: false` を維持しつつ、preload の agent 別ファイル分割のために `sandbox: false` としている。renderer に公開する API は `window.agentCliDevkit` 経由の明示的なブリッジのみ。
- クリーンアップ・削除操作はすべて既定 OFF のチェックボックス + 確認ダイアログを経る。使用中ファイルは best-effort でスキップし、結果として報告する。
